# articles/

外部プラットフォーム向けの記事下書き。**プロジェクトのドキュメントではない。**

拡張そのものの説明を探しているなら [`../docs/README.md`](../docs/README.md) を見る。

| ファイル | 公開先 | 状態 |
| --- | --- | --- |
| `e2e-browser-extension-playwright.md` | Zenn | 下書き（frontmatter は `published: false`） |

## 注意

- ここのファイルは `yarn check` のどの検査対象にも入っていない。ストア掲載文（`docs/store-listing/`）と違って、拡張 ID・ユーザー数・コマンド名がずれても誰も教えてくれない。
- `published: true` に変える前に、記事中の事実を今のリポジトリと突き合わせる。特に拡張 ID、ユーザー数、`package.json` のスクリプト名、Playwright のプロジェクト名。
- 記事は日本語で書く。公開ドキュメントの英語統一とは別扱い。
