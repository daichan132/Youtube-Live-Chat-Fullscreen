nodeLinker: node-modules

# Delay newly published package versions during normal local resolution.
# Renovate applies the same three-day minimum before opening update PRs.
npmMinimalAgeGate: 4320m

yarnPath: .yarn/releases/yarn-4.18.0.cjs
