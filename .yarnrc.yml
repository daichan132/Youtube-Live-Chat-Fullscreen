nodeLinker: node-modules

# `yarn up` bypasses Yarn's resolver gate; CI also verifies new yarn.lock npm locators with verify:dependency-age.
npmMinimalAgeGate: 4320m

yarnPath: .yarn/releases/yarn-4.18.0.cjs
